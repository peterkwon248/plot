Run `git pull` to sync with the remote repository before starting work.

Steps:
1. Run `git status` to check current state
2. Run `git pull` to fetch and merge latest changes
3. Report the result (up-to-date, merged changes, or conflicts)
